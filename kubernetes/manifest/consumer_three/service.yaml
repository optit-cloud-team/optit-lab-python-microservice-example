apiVersion: v1
kind: Service
metadata:
  name: consumer-three-service
  namespace: bktest
spec:
  selector:
    app: consumer-three
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: NodePort
